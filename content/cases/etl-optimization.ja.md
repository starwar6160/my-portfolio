---
title: "ETL最適化：バッチ処理を70秒から1.5秒へ（50倍の高速化）"
date: 2026-03-10
tags: ["Performance", "Architecture", "Database", "Go", "ETL"]
description: "同期的なレガシーパイプラインを、高並列・非同期なバッチアップサートアーキテクチャに再設計。ミッションクリティカルなデータ整合性を維持しつつ、スループットを50倍向上させた事例。"
---

## プロジェクト背景

ある大規模金融データ処理システムにおいて、増大するトランザクション量により既存のETL（抽出・変換・格納）プロセスが限界に達していました。1つのバッチ処理に**70秒**を要しており、データ同期の遅延がビジネスの意思決定を阻害する重大なリスクとなっていました。

私のミッションは、システムのコードを1行ずつ「解剖」し、ボトルネックを根本から排除して、リアルタイムに近いパフォーマンスを実現することでした。

---

## 課題 (Technical Bottlenecks)

1.  **同期的な「1件ずつ」の処理**: 従来のループ処理が、1件の挿入ごとにデータベースのラウンドトリップを待機していた。
2.  **データベースのロック競合**: 頻繁な小規模アクセスのため、インデックスの書き換えによるデッドロックが多発。
3.  **非効率なメモリ管理**: 大量データ読み込み時にGC（ガベージコレクション）が頻発し、CPU時間の30%を消費。

---

## 採用したアーキテクチャ・ソリューション

### 1. 「非同期バッチ・アップサート」への転換
同期処理を完全に廃止し、**Worker Pool**モデルを採用しました。
- 特定の時間、または件数のしきい値に達するまでメモリ内のバッファにデータを蓄積。
- PostgreSQLの `INSERT ... ON CONFLICT` (UPSERT) を活用し、1回のトランザクションで数千件を処理。
- これにより、ネットワークのオーバーヘッドを99%削減しました。

### 2. メモリ再利用とゼロコピーの徹底
ガベージコレクションの負荷を抑えるため、Goの `sync.Pool` を使用してバッファを再利用。
- データのデコードプロセスにおいてメモリアロケーションを極限まで抑え、大容量データ処理時でも低遅延を維持。

### 3. 可観測性 (Observability) の導入
単に速くするだけでなく、常に安定して動作することを保証しました。
- Prometheus + Grafanaによるカスタム・メトリクスの導入。
- 各ステージのレイテンシを可視化し、将来的なボトルネックの予兆を事前に把握可能に。

---

## 事業における成果 (Business Results)

- **劇的なスループット向上**: 処理時間が **70秒から1.5秒** へ。エンジニアリングによる50倍のパフォーマンス改善を達成。
- **インフラコストの最適化**: 以前は10台必要だった処理ノードを2台に集約。クラウドサーバーの運用コストを大幅に削減。
- **データ鮮度の向上**: ビジネスサイドは、ほぼリアルタイムのデータに基づいて意思決定を行えるようになり、市場への反応速度が向上。

---

> [!TIP]
> **黄金律**: 高性能インフラとは、リソースを「追加」することではなく、既存リソースの「無駄」を数学的に削ぎ落とすことによって完成します。

*あなたのシステムのパフォーマンス限界を突破しませんか？*
